<h1>ジャンル一覧・追加</h1>
<h4>ジャンル名</h4>
<%= form_with model: @genre, local:true do |f| %>
<%= f.text_field :name %>
<%= f.submit '新規登録' %>
<% end %>

<table>
<% @genres.each do |genre| %>
  <tr>
    <td><%= genre.name %></td>

  </tr>
  <% end %>
</table>
